<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>com.b2international.snowowl</groupId>
		<artifactId>releng-parent</artifactId>
		<version>7.0.0-SNAPSHOT</version>
	</parent>

	<artifactId>target-platform</artifactId>
	<packaging>eclipse-target-definition</packaging>

	<build>
		<plugins>
			<plugin>
				<groupId>lt.velykis.maven</groupId>
				<artifactId>pde-target-maven-plugin</artifactId>
				<version>1.0.0</version>
				<executions>
					<execution>
						<id>pde-target</id>
						<goals>
							<goal>add-pom-dependencies</goal>
						</goals>
						<configuration>
							<baseDefinition>${project.artifactId}.target</baseDefinition>
							<outputFile>${project.artifactId}-local.target</outputFile>
						</configuration>
					</execution>
				</executions>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-resources-plugin</artifactId>
				<version>2.4.3</version>
				<executions>
					<execution>
            			<id>copy-snow-owl-config-users</id>
            			<phase>process-classes</phase>
            			<goals>
              				<goal>copy-resources</goal>
            			</goals>
            			<configuration>
            				<resources>
            					<resource>
                  					<directory>../com.b2international.snowowl.server.update/assembly/common/configuration</directory>
                  					<includes>
                            			<include>users</include>
                        			</includes>
                				</resource>
            				</resources>
            				<outputDirectory>${basedir}/configuration</outputDirectory>
            			</configuration>
          			</execution>
          			<execution>
            			<id>copy-snow-owl-config</id>
            			<phase>process-classes</phase>
            			<goals>
              				<goal>copy-resources</goal>
            			</goals>
            			<configuration>
            				<resources>
            					<resource>
                  					<directory>../com.b2international.snowowl.server.update/assembly/h2</directory>
                  					<includes>
                            			<include>snowowl_config.yml</include>
                        			</includes>
                				</resource>
            				</resources>
            				<outputDirectory>${basedir}</outputDirectory>
            			</configuration>
          			</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

</project>