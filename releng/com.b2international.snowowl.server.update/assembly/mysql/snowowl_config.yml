#
# Copyright 2011-2015 B2i Healthcare Pte Ltd, http://b2i.sg
#

authentication:
  type: PROP_FILE

fileAuth:
  users:
    -
      username: snowowl
      password: snowowl

repository:
  host: 0.0.0.0
  port: 2036
  # Index timeout to invalidate and purge the unused indexes for tasks
  # Default is 240, setting it to 0 means infinite (turns it off)
  # indexTimeout=240
  database:
    type: b2i_mysql
    datasource: com.mysql.jdbc.jdbc2.optional.MysqlDataSource
    driver: com.mysql.jdbc.Driver
    scheme: jdbc:mysql://
    location: localhost:3306/
    settings: ?useServerPrepStmts=true&useCursorFetch=true
    username: snowowl
    password: snowowl
