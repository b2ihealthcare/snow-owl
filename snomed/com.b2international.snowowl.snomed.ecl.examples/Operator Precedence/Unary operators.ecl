/* Unary operators (e.g. <, <<, >, >>, ^) are applied from inside to out (i.e. from 
 * right to left). For example, when the following expression constraint is processed,
 * the ^ operator is applied first to the ICD-10 complex map reference set, and then
 * the descendants of the referenced components are determined.
 */
 
<^447562003|ICD-10 complex map reference set|