/* The following expression constraint is satisfied by the set of concepts which 
 * are members of the |ICD-10 complex map reference set|. That is, these concepts
 * have all been mapped to ICD-10.
 */

^447562003|ICD-10 complex map reference set|