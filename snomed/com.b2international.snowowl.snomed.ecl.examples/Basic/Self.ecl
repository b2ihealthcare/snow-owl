/* If no expression constraint operator or membership function is applied,
 * the expression constraint is satisfied only by the specified concept. 
 * For example, the expression constraint below is satisfied only by the 
 * concept 404684003|Clinical finding|.
 */
 
404684003|Clinical finding|