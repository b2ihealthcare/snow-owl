/* The 'star' symbol (i.e. "*") may be used to represent any attribute value 
 * (either with or without refinement). The following expression constraint 
 * evaluates to the set of clinical findings which have an associated morphology 
 * (with any value).
 */
 
<404684003|Clinical finding|: 116676008|Associated morphology| = *